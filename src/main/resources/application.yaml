spring:
  application:
    name: curalink

  # Database setup
  datasource:
    url: jdbc:postgresql://localhost:5432/curalink
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Jwt configuration
  jwt:
    secret:
      key: z(#{?8qEs0R_kJ_2)OH@<G.1.m&W#^w9ehNf55p-/M<