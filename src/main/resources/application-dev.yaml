spring:
  jpa:
    properties:
      # Migrations generation config
      javax:
        persistence:
          schema-generation:
            scripts:
              action: create
              create-target: src/main/resources/db/migration/V1__init__schema.sql # generate V1 of the datbase